#include <stdio.h>

void main ()
{
int number;
printf("Enter a value")
scanf("%d", &num);

if (num % 2 == 0) {
    printf("%d is an even number");
    else 
    printf("%d is an odd number");
}
    return 0;
}